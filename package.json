{
  "name": "sysupdate_server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "dev": "bun run --watch main.ts",
    "lint": "bunx biome lint --write",
    "formatter": "bunx biome format --write",
    "check": "bunx biome check --write",
    "format": "bun check && bun lint && bun formatter"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/semver": "^7.7.1",
    "biome": "^0.3.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "elysia": "^1.4.28",
    "semver": "^7.7.4"
  }
}
